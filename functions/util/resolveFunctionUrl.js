// let resolvedRequestUrl = '/'
// try {
//   if (!req.originalUrl.includes(FIREBASE_FUNCTION_URL))
//     throw new Error(`${FIREBASE_FUNCTION_URL} is not in ${req.originalUrl}`)
//   resolvedRequestUrl = req.originalUrl.replace(FIREBASE_FUNCTION_URL, '')
// } catch (error) {
//   console.error('> failed to resolve request url', error)
// }
